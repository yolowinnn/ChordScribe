rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 每个用户只能读写自己的谱库文档 chordscribe/{uid}
    match /chordscribe/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
